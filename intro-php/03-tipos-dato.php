<?php include 'includes/header.php';

// Booleano
$loggedIn = false;
var_dump($loggedIn);

// Enteros
$edad = 25;
var_dump($edad);

// Flotantes
$altura = 1.75;
var_dump($altura);

// Strings
$nombre = 'Alvaro';
var_dump($nombre);

// Arreglos
$colores = ['rojo', 'azul', 'verde'];
var_dump($colores);


include 'includes/footer.php';

