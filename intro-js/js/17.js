/*
 * Diferencia entre metodos y
 * funciones.
 */

const numero1 = 10;
const numero2 = '20';

// Funcion:
// No esta ligado a un tipo de dato
// recoje parametros y los manipula
// se ejecutan globalmente sin pertenecer
// a ningun otro simbolo

// Metodo:
// Se utiliza en tipos de datos
// para manipular estos y se ejecutan
// con un . despues de el nombre de la variable

// parseInt(numero, rango de numero ej: 12 - 10, 102, 100)
console.log(parseInt(numero2, 10));
console.log(numero1.toString());
