// Strings
const tweet = 'Aprendiendo JS con Juan';
const producto2 = String('Monitor 30 pulgadas');
const email = 'correo@gmail.com';

console.log(producto2.length());

// Retorna en que caracter se encuentra
console.log(tweet.indexOf('JS'));
console.log(email.indexOf('@'));

// Retorna si existe en el String
console.log(producto2.includes('30'));
